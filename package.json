{
  "name": "react-browser-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "./build.sh",
    "zip": "(rm packed.zip || true) && zip -r packed.zip build/"
  },
  "workspaces": [
    "content_scripts/app",
    "options",
    "popup"
  ],
  "devDependencies": {
    "typescript": "^4.8.4",
    "web-ext": "^6.6.0"
  }
}
